name: repoaudit

on:
  push:
    branches: [main]
  pull_request:

jobs:
  score:
    name: score
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./
        with:
          path: .
          markdown: "true"
